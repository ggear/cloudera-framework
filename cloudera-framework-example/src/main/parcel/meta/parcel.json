{
	"schema_version": 1,
	"name": "${parcel.name}",
	"version": "${parcel.version}",
	"extraVersionInfo": {
		"fullVersion": "${parcel.version.full}",
		"baseVersion": "${parcel.version.base}"
	},
	"setActiveSymlink": true,
	"scripts": {
		"defines": "cloudera-framework-example-env.sh"
	},
	"packages": [{
		"name": "${parcel.label}",
		"version": "${parcel.version}"
	}],
	"components": [{
		"name": "${parcel.label}",
		"version": "${parcel.version}",
		"pkg_version": "${parcel.version}"
	}],
	"provides": [
		"cdh-plugin",
		"${parcel.label}"
	],
	"users": {},
	"groups": []
}